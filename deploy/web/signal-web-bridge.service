[Unit]
Description=Signal Web Bridge
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/signal-web/server
EnvironmentFile=/etc/signal-web/bridge.env
ExecStart=/usr/bin/env pnpm start
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
